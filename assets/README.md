# Assets Directory

Place your `intro.mp4` video here.
If `intro.mp4` is present, the Home screen displays an intro video background.
If the video file is missing or cannot be loaded, JobFinder AI automatically activates the fallback hero banner with animated gradient elements, ensuring the app never crashes.
